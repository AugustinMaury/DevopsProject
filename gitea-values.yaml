gitea:
  admin:
    username: admin
    password: admin123
    email: admin@example.com

service:
  http:
    type: ClusterIP
    port: 3000
  ssh:
    type: NodePort
    port: 22

ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
  hosts:
    - host: gitea.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

persistence:
  enabled: true
  size: 10Gi
  storageClass: standard